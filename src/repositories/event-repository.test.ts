import { beforeAll, describe, it } from 'vitest';

describe('events repository', () => {
	beforeAll(() => {
		// TODO: setup database connection and delete the events
	});

	it('should create a new stream', () => {
		// TODO
	});

	it('should add a new event to the stream', () => {
		// TODO
	});

	it('should get the event stream', () => {
		// TODO
	});
});
